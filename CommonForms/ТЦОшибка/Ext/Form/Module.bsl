
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Описание = Параметры.Ошибка.Описание;
	
КонецПроцедуры


&НаКлиенте
Процедура Подробнее(Команда)
	
	ПараметрыФормы = Новый Структура;
	ПараметрыФормы.Вставить("Ошибка", Параметры.Ошибка);
	ПараметрыФормы.Вставить("ДатаНачала", Параметры.ДатаНачала - 3);
	ПараметрыФормы.Вставить("ДатаОкончания", Параметры.ДатаОкончания + 3);
	
	ОткрытьФорму("ОбщаяФорма.ТЦОшибкаПодробнее", ПараметрыФормы);
	
КонецПроцедуры

